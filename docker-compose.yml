services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - order_data:/app/data
    environment:
      - NUXT_SESSION_PASSWORD=change-this-to-a-random-32-char-secret
    restart: unless-stopped

volumes:
  order_data:
