services:
  app:
    container_name: ShipmentTracking
    build: .
    ports:
      - "3333:3000"
    volumes:
      - ./persist/data:/app/data
    environment:
      - NUXT_SESSION_PASSWORD=change-this-to-a-random-32-char-secret
    restart: unless-stopped
