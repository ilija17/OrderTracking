# Session encryption secret — must be at least 32 characters
# Generate with: openssl rand -base64 32
NUXT_SESSION_PASSWORD=

# Optional: set the initial admin password on first run (when no users exist)
# If not set, a random password will be generated and printed to the console
# NUXT_INITIAL_ADMIN_PASSWORD=
